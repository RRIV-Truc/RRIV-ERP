/**
 * Push notification — chưa triển khai trên RRIV/Supabase (stub)
 */
const FCMHandler = {
  async init() { return false; },
  async requestPermission() { return null; },
  async saveToken() { return false; }
};
